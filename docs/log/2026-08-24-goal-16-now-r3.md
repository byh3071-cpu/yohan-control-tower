# Goal 16 · 실데이터 NOW-R3 구현

- Date: 2026-08-24 (Asia/Seoul)
- Branch: `codex/control-tower-design-direction`
- Phase: 관제탑 실행 화면 전환
- Goal: 실데이터 기반 NOW-R3 지금 화면 구현
- Status: 구현·검증 완료, VHK 2.14.0 재검증 완료, Git 반영 전

## 결과

- 기존 상단 5탭을 유지하고 Home 진입을 승인된 NOW-R3 구조로 교체했다.
- `projects.yaml → active project → local repo/goals`에서 Goal과 Completion Check Task를 읽는다.
- 활성 Goal 0개·1개·복수를 분리하고 복수일 때 하나를 임의 선택하지 않는다.
- Goal 22 live 상태에서 Task 2/5, 완료 1/5, 다음 Task를 확인했다.
- local project coverage를 `로컬 7 / 18`로 표시해 미클론 상태를 숨기지 않는다.

## 검증

- TypeScript: PASS
- ESLint: PASS · 오류 0, 기존 경고만 존재
- Node test: PASS · 71/71
- Next production build: PASS · 기존 NFT 과추적 경고 1건
- VHK Goal 16 check: PASS
- VHK 2.14.0 verify: PASS · 5/5 (`typecheck`, `lint`, `test`, `build`, `secure scan`)
- Browser: 360·432·768·1280·1440px overflow 0, H1 1, 최소 14px, primary 176×44
- Console: error 0, warning 0
- Keyboard: primary focus ring 확인, Enter로 프로젝트 탭 이동
- State fixture: single·multiple·empty·error·loading PASS
- 상세: `docs/design/control-tower-vnext/design-qa.md`

## 적대검토 보정

- 프로젝트 key를 filesystem path에 합치기 전에 `isSafeRepoSlug`를 적용했다.
- Brain `goals/`만 읽던 기존 API를 configured active local project Goal 계층으로 교정했다.
- direct Goal link가 아닌 행동을 `Goal 전체 보기`라고 부르던 문구를 `프로젝트에서 확인`으로 고쳤다.

## Orca 런타임 관찰

- 새 Run `run_c9c9407f7f62`와 읽기 전용 Scout Task를 만들었다.
- Codex worker는 `codex-update-prompt`에서 준비 실패했고 Claude worker 재시도는 접수됐다.
- 이후 Orca app은 running이지만 runtime·graph가 계속 `starting`이었고 PID가 `64296 → 64708`로 바뀌었다.
- runtime ready가 아니어서 추가 orchestration RPC·critic worker를 만들지 않았다.
- Scout 결과는 회수하지 못했고 로컬 `cross-check` 계약으로 적대검토를 대체했다.

## 남은 사람 게이트

- 현재 미커밋 diff와 QA 캡처 검토
- commit·push 여부 결정
- Notion `바이브코딩 Dev Log` 적재 승인
- Goal 22의 `IN_PROGRESS` frontmatter와 본문의 `보류` 의미 불일치 정리 여부 결정
